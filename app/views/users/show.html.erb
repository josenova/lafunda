<h1><%= t('views.welcome') + ', ' + @current_user.name %></h1>


<p>
  <strong>Name:</strong>
  <%= @current_user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @current_user.email %>
</p>

<%= link_to 'Edit', edit_account_path %>




<% content_for :modules do %>
    <div id="balance">
      <span>Balance:</span>
      <p>RD$<%= @current_user_funds %></p>
      <a href="#" id="deposit">Deposit</a>
      <a href="#" id="withdraw">Withdraw</a>
    </div>
<% end %>